import json
import os
import re
from pathlib import Path

# Paths
base_path = Path("/home/veer/Ranveer/GlobeCore")
blogs_json_path = base_path / "blogshtml" / "blogs.json"
# Note: I deleted blogshtml folder in previous turn, wait! I should have kept the json.
# Let me check if it still exists or if I should recover it.
# Actually I might have deleted it. Let me check.
